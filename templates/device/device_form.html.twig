{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit'|trans }} {{ 'Device'|trans }}{% endblock %}

{% block body %}
<div class="row align-items-center">
  <div class="col-auto">
    {% if image %}
    <img style="width: 200px;" src="{{ image }}" alt="Device image" />
    {% endif %}
  </div>
  <div class="col">
    <h1>{{ 'Edit'|trans }} {{ 'Device'|trans }}</h1>
  </div>
</div>
  {{ form(deviceForm) }}
{% endblock %}
